# source: http://localhost:4000
# timestamp: Mon Apr 22 2019 00:28:01 GMT-0700 (Pacific Daylight Time)

directive @cacheControl(maxAge: Int, scope: CacheControlScope) on FIELD_DEFINITION | OBJECT | INTERFACE

type Author {
  email: String
  name: String
  id: ID!
}

input authorInput {
  email: String
  name: String
}

enum CacheControlScope {
  PUBLIC
  PRIVATE
}

type Mutation {
  createAuthor(input: authorInput): Author!
  createPost(input: postInput): Post!
}

type Post {
  title: String
  author: String
  content: String
  id: ID!
}

input postInput {
  title: String
  author: String
  content: String
}

type Query {
  authors: [Author]
  posts: [Post]
  getPost(id: ID): Post
  getAuthor(id: ID): Author
}

"""The `Upload` scalar type represents a file upload."""
scalar Upload
